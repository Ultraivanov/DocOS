# DocOS — Design (UX/UI) ## 1. UX Principles - loop-first design - zero friction - immediate feedback --- ## 2. Primary Flow Landing → Create → 
Generate → Edit → Run → Compare → Iterate --- ## 3. Layout Main screen: - Left: Config editor - Right: Output - Bottom: Test input + Run --- ## 
4. Core Components - Config textarea - Test input - Run button - Output panel --- ## 5. Interaction 1. User writes config 2. User enters input 3. 
Clicks Run 4. Sees output 5. Edits config 6. Repeats --- ## 6. UX Goal The product should feel like:
> fast iteration loop to improve AI behavior
